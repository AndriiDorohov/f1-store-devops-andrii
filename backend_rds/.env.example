# Connection to PostgreSQL (service 'postgres' from docker-compose.yml)
DB_NAME=f1store
DB_USER=f1user
DB_PASSWORD=change_me
DB_HOST=postgres
DB_PORT=5432

# CORS — for local development (frontend on port 8080)
CORS_ALLOWED_ORIGINS=http://localhost:8080
